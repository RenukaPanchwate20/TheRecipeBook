{
  "name": "markstory",
  "version": "1.0.0",
  "description": "",
  "main": "add-recipe.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev-sync": "browser-sync start --server --files \"*.html, *.css, *.js\" --port 3001",
    "dev-full": "concurrently \"npm run dev\" \"npm run dev-sync\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.2"
  }
}
